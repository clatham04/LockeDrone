# signal_controller — Python dependencies
# Install on the Raspberry Pi:  pip install -r requirements.txt

# --- RF link (needed now) ---
pyrf24                  # nRF24L01+ driver over SPI — used by test_radio.py and drone_link.py

# --- Anti-drift camera loop (coming next) ---
opencv-contrib-python   # camera + ArUco marker tracking for true position hold
numpy                   # math for the control loop
