# signal_controller — Python dependencies
# Install on the Raspberry Pi:  pip install -r requirements.txt

# Core flight control (signals_control.py / wifi_control.py / wifi_connect.py) needs
# NO external packages — just the Python standard library + NetworkManager (nmcli).

# --- Camera (camera_feed.py: the drone's forward RTSP video) ---
opencv-python           # cv2 — pull/decode the RTSP stream

# --- Future: human-follow behavior (downward camera + YOLO) ---
# ultralytics           # YOLO person detection
# numpy
